import type { NextConfig } from "next";

/**
 * nextConfig: Next.js projesinin teknik ayarlarının yapıldığı nesne.
 */
const nextConfig: NextConfig = {
  /* 
     Buraya projenin ihtiyacına göre çeşitli ayarlar eklenebilir.
     Örneğin:
     - images: Dış kaynaklardan gelen resimlere (domain) izin vermek için.
     - redirects: Sayfalar arası kalıcı yönlendirmeler için.
     - env: Proje genelinde kullanılacak ortam değişkenleri için.
     - typescript: Derleme sırasında TS hatalarını göz ardı etmek gerekirse (pek önerilmez).
  */
};

export default nextConfig;